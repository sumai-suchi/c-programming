#include <stdio.h>
int main()
{
    /* int dewan, mahin;
    char c;
    float f;
    scanf("%d %f %c", &dewan, &f, &c);
    printf("%d %0.2f %c", dewan, f, c);
    //scanf("%d %d", &dewan, &mahin);
    //printf("%d %d", dewan, mahin); */
    //--- take % as a input
    int a, b;
    /* char p;
    scanf("%d%c %d%c", &a, &p, &b, &p);
    printf("%d%% %d%%", a, b); */
    // ---other way
    scanf("%d%% %d%%", &a, &b);
    printf("%d%% %d%%", a, b);

    return 0;
}
